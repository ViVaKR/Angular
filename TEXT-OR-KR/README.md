# TEXT.OR.KR

```bash
ng serve
``

(icon)[https://fonts.google.com/icons?icon.set=Material+Icons&icon.style=Filled&icon.size=24&icon.color=%231f1f1f&selected=Material+Icons:menu_open:&icon.query=hamburger+menu]

나의 정보
권한관리
회워관리
과목관리
로그아웃

  @switch (condition) {
    @case (caseA) {
      Case A.
    }

    @case (caseB) {
      Case B
    }

    @default {
      Default case.
    }
  }

  @let user = user$ | async


@if (a > b) {
  {{a}} is greater than {{b}}
} @else if (b > a) {
  {{a}} is less than {{b}}
} @else {
  {{a}} is equal to {{b}}
}


    $count : 컬렉션의 항목 수를 반복합니다.
    $index : 현재 행의 인덱스
    $first : 현재 행이 첫 번째 행인지 여부
    $last : 현재 행이 마지막 행인지 여부
    $even : 현재 행이 짝수 행인지 여부
    $odd : 현재 행이 홀수 행인지 여부
    @if(e) { <p> 짝수 </p> }

```
